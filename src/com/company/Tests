package com.company;

import com.company.Main;
//Info info = new Info("B",2);
        //Info info1 = new Info("D", 4);
        //Info info2 = new Info("C", 3);
        //Info info3 = new Info("A", 1);
        //Info info4 = new Info("C", 5);
        //HashMap<String, List<Info>> vertexMap = new HashMap<>();
        //vertexMap.put("A",List.of(info, info4));
        //vertexMap.put("B", List.of(info1));
        //vertexMap.put("D", List.of(info3, info2));
        //vertexMap.put("C", List.of());
        //this.vertexMap = vertexMap;
//* vertexMap.remove(vertexMap.get(vertexName1));
//			vertexMap.put(vertexName1,edges1);
//* public String getInfo() {
//	return String.valueOf(vertex)+String.valueOf(weight);
//	}

//* for (List<Info> value : vertexMap.values()) {
//				value.removeIf(each -> each.getVertex().equals(vertexName));
//			}
//(vertexMap.containsKey(vertexName1))

//* Set<Map.Entry<String, List<Info>>> entrySet = vertexMap.entrySet();